%nav.navbar.navbar-light.frontend-navbar
  %ul.nav.navbar-nav
    %li.nav-item
      = link_to backend_users_path, class: 'nav-link' do
        = t('backend.pages.users.title')
    %li.nav-item
      = link_to backend_kuwasys_root_path, class: 'nav-link' do
        = t('backend.pages.kuwasys.title')
    %li.nav-item
      = link_to backend_schoolyears_path, class: 'nav-link' do
        = t('backend.pages.schoolyears.title')
  .pull-right
    - if policy(:backend).index?
      = link_to root_path, class: 'btn btn-primary-outline' do
        = t('frontend')
    = link_to(destroy_user_session_path, class: 'btn btn-danger-outline') do
      = t('logout')
